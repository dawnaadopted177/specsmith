# Failure-Mode Graph — {{ project.name }}

This document contains the Failure-Mode Graph (G) for {{ project.name }}: the mapping of
stress-test → breakpoint relations discovered during AEE stress-testing.

Failure modes are not defects — they are **recovery opportunities**. Each mode has a
recovery hint and a corresponding `RecoveryProposal` emitted by `RecoveryOperator`.

Last generated: Run `specsmith stress-test` to update.

---

## Equilibrium Status

```
Equilibrium: [Run specsmith epistemic-audit to check]
Logic Knots: [Run specsmith stress-test to detect]
```

When `equilibrium: YES`, the belief system has no unresolved critical failures and
no Logic Knots. This is the target state before milestone gates.

---

## Active Failure Modes

Failure modes are organized by severity: CRITICAL → HIGH → MEDIUM → LOW.

<!-- This section is auto-generated by specsmith stress-test.
     Manually review and annotate resolved items.

### CRITICAL

| Artifact ID | Challenge | Breakpoint | Recovery Hint | Status |
|-------------|-----------|------------|---------------|--------|
| REQ-XXX-001 | ... | ... | ... | ☐ open / ☑ resolved |

### HIGH

| Artifact ID | Challenge | Breakpoint | Recovery Hint | Status |
|-------------|-----------|------------|---------------|--------|

### MEDIUM

| Artifact ID | Challenge | Breakpoint | Recovery Hint | Status |
|-------------|-----------|------------|---------------|--------|

### LOW

| Artifact ID | Challenge | Breakpoint | Recovery Hint | Status |
|-------------|-----------|------------|---------------|--------|
-->

No failure modes documented yet. Run `specsmith stress-test` to populate.

---

## Logic Knots

Logic Knots are irreducible conflicts between accepted beliefs. Each knot is a stop
condition that must be resolved before the belief system can reach equilibrium.

<!-- Format:
### KNOT-001: [ID1] ↔ [ID2]

- **Reason**: [Why these two beliefs conflict]
- **Status**: open | resolved
- **Resolution**: [How it was resolved — or empty if open]
- **Resolved by**: [human operator]
- **Date resolved**: YYYY-MM-DD
-->

No logic knots detected. Run `specsmith stress-test --accepted-only` to check.

---

## Mermaid Diagram

Run `specsmith stress-test --format mermaid` to generate a Mermaid diagram
of the full failure-mode graph that can be embedded here.

---

## Recovery History

Record of recovery actions applied. Each entry links to a LEDGER.md entry.

<!-- Format:
| Date | Artifact | Failure Mode | Strategy | LEDGER entry |
|------|----------|--------------|----------|--------------|
| YYYY-MM-DD | REQ-XXX | ... | QUANTIFY | 2026-01-15 — ... |
-->
